//! Minimal test binary for ggml FFI.
//! Run with: cd ffi && cargo run

fn main() {
    println!("=== ggml FFI test ===");
    println!("This crate is a library. Run tests with: cargo test");
    println!("=== done ===");
}
